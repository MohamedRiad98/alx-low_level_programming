#include "main.h"
/**
 * This project will print the output of alphabet in uppercase.
 * @c: int
 * Return 1 if upper 0 if else
 */
int main()
{
	int _isupper(int c)
	{
		char a
		for(a = 'A'; a = 'Z'; a++)
		{
			if(a == c)
			{
				printf("%c is upper charactor", a);
				return(1);
			}
		}
		return(0);
	}
}
