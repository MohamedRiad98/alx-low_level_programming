#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, except for q and e.
 *
 * Return: Always 0.
 */
int main(void)
{
	int number;

	for (number = '0'; number <= '9'; number++)
	{
	putchar(i);

	}
	putchar('\n');
	return (0);
}
